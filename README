TO COMPILE
-Windows-
From assignment folder, run the following command in command prompt: javac -d ./ src/*.javac
-Linux-
From assignment folder, run the following command in the terminal: make

TO RUN
-Both Windows and Linux-
From assignment folder, run the following command: java dishWashS.CleaningDishes <size of dishes> <rack size> <washer slackness> <dryer slackness>